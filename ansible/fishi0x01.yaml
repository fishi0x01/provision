---
- hosts: localhost
  gather_facts: false
  become: true
  roles:
    - pre

- hosts: localhost
  become: true
  roles:
    - system
    - python
    - solokey
    - virtualbox
    - keybase
    - docker
    - arduino_ide
    - fritzing_ide
    - eagle_ide
    - flutter
    - android_studio
    - gvm
    - nvm
    - r
    - rust
